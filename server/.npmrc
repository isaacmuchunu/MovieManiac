# Enforce Node.js engine requirements from package.json
# npm install will fail if Node.js version doesn't match engines.node
engine-strict=true

# Use exact versions by default for better reproducibility
save-exact=true

# Disable package-lock update notifications
fund=false
